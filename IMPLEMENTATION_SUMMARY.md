# خلاصه تغییرات انجام شده

## ۱. رفع مشکل تغییر رمز عبور
- **فایل**: `/workspace/components/ProfileModal.jsx`
- **تغییرات**: تابع `handleChangePassword` به‌روزرسانی شد تا از API جدید `/api/change-password` استفاده کند
- **ویژگی‌ها**:
  - بررسی رمز عبور فعلی
  - هش کردن رمز جدید با bcrypt
  - نمایش پیام موفقیت یا خطا

## ۲. ایجاد API تغییر رمز عبور
- **فایل**: `/workspace/app/api/change-password/route.js`
- **عملکرد**: دریافت رمز قدیمی، بررسی آن، هش کردن رمز جدید و آپدیت در دیتابیس

## ۳. سیستم خرید کانفیگ
### الف) جدول دیتابیس purchases
- **فایل SQL**: `/workspace/supabase_schema.sql`
- **فیلدها**:
  - `id`: شناسه خرید
  - `user_id`: شناسه کاربر
  - `product_id`: شناسه محصول
  - `config_link`: لینک کانفیگ (توسط ادمین پر می‌شود)
  - `status`: وضعیت (pending, completed, cancelled)
  - `created_at`: تاریخ خرید

### ب) API خرید
- **فایل**: `/workspace/app/api/purchases/route.js`
- **GET**: دریافت خریدهای کاربر
- **POST**: ثبت خرید جدید (بررسی موجودی، کسر از کیف پول، ثبت خرید)

### ج) API آپدیت لینک کانفیگ
- **فایل**: `/workspace/app/api/purchases/update-config/route.js`
- **PUT**: ادمین می‌تواند لینک کانفیگ را برای خرید ثبت کند

## ۴. افزودن قابلیت خرید به صفحه محصولات
- **فایل**: `/workspace/components/ProductsList.jsx`
- **ویژگی‌ها**:
  - دکمه "خرید کانفیگ" حالا لاگین را چک می‌کند
  - مودال تأیید خرید با نمایش جزئیات محصول
  - بررسی موجودی کیف پول
  - کسر خودکار از کیف پول
  - ثبت خرید در دیتابیس

## ۵. بخش کانفیگ‌های خریداری شده در پروفایل
- **فایل**: `/workspace/components/ProfileModal.jsx`
- **ویژگی‌ها**:
  - نمایش لیست خریدهای کاربر
  - نمایش وضعیت هر خرید (در انتظار، تکمیل شده، لغو شده)
  - نمایش لینک کانفیگ پس از تایید ادمین
  - دکمه کپی لینک کانفیگ
  - طراحی زیبا و ریسپانسیو

## ۶. پنل ادمین برای مدیریت خریدها
- **نیاز به اضافه شدن به**: `/workspace/app/admin/page.js`
- **ویژگی‌های پیشنهادی**:
  - نمایش لیست خریدهای در انتظار
  - فیلد ورودی برای قرار دادن لینک کانفیگ
  - دکمه تأیید و ارسال لینک به کاربر

---

## نحوه استفاده:

### برای کاربر:
1. وارد حساب کاربری شوید
2. از صفحه اصلی محصول مورد نظر را انتخاب کنید
3. روی "خرید کانفیگ" کلیک کنید
4. اگر موجودی کافی دارید، خرید ثبت می‌شود
5. در پروفایل خود، بخش "کانفیگ‌های خریداری شده" را چک کنید
6. پس از تایید ادمین، لینک کانفیگ را مشاهده و کپی کنید

### برای ادمین:
1. به پنل مدیریت بروید
2. خریدهای در انتظار را مشاهده کنید
3. لینک کانفیگ را در فیلد مربوطه قرار دهید
4. وضعیت را به "تکمیل شده" تغییر دهید
5. کاربر می‌تواند لینک را در پروفایل خود ببیند

---

## فایل‌های ایجاد شده:
- `/workspace/supabase_schema.sql` - اسکریپت ساخت جدول purchases
- `/workspace/app/api/purchases/route.js` - API خریدها
- `/workspace/app/api/purchases/update-config/route.js` - API آپدیت لینک کانفیگ
- `/workspace/app/api/change-password/route.js` - API تغییر رمز عبور

## فایل‌های ویرایش شده:
- `/workspace/components/ProfileModal.jsx` - افزودن بخش کانفیگ‌ها + رفع باگ تغییر رمز
- `/workspace/components/ProductsList.jsx` - افزودن قابلیت خرید
